tareas_dia = ["Revisar Email", "Llamar al cliente", "comprar café", "Actualizar reporte", "Planificar reunión"]

# tareas_orden = sorted(tareas_dia, key=str.lower)
tareas_orden = sorted(tareas_dia, key=len.sort)

print(tareas_dia)

print(tareas_orden)